/* Programa del servicio social*/
#include<stdio.h>

	int main () {
	

		int i,j;	
		FILE *fp
		float matriz[N][N];
		float x[N],y[N];
		fp = fopen(".txt","r");
		
			fscanf(fp,"%f %f",&x,&y);

		fclose (fp);
		
		for(i=0; i<=x; i++){
			for(j=0; j<=x ; j++);{
				
				matriz[i][j];
	
